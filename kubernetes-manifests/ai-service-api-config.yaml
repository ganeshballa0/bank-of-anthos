apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-service-api-config
  namespace: default
  labels:
    application: bank-of-anthos
    environment: development
    team: frontend
    tier: web
data:
  # --- Primary AI service used by aifrontend ---
  # host:port (scheme is added in code as "http://")
  AISERVICES_API_ADDR: aiservices:8000

  # --- (Optional) Future AI endpoints: add here as you onboard them ---
  # AICHAT_API_ADDR: aichat:8080
  # AIEMBEDDINGS_API_ADDR: aiembeddings:8081
  # AIVISION_API_ADDR: aivision:8082
  # AIVECTOR_API_ADDR: aivector:8090
