# MCP Server Dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy code
COPY mcp_server.py .

# Expose port (MCP server runs here)
EXPOSE 8000

# Run MCP server
CMD ["python", "mcp_server.py"]
